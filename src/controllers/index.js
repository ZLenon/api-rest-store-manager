const {
  findAllControler,
  findByIDControler,
  createProductControler,
} = require('./productsControllers');

module.exports = {
  findAllControler,
  findByIDControler,
  createProductControler,
};