const {
  findAllControler,
  findByIDControler,
  createProductControler,
} = require('./productsControllers');

const {
  createSaleController,
} = require('./salesControllers');

module.exports = {
  findAllControler,
  findByIDControler,
  createProductControler,
  createSaleController,
};